namespace: fleet-mc-helm-external-example-nvidia
helm:
  chart: https://github.com/NVIDIA/gpu-operator/archive/refs/tags/v1.7.1.tar.gz
targetCustomizations:
- name: dev
  helm:
    values:
  clusterSelector:
    matchLabels:
      env: gpu
